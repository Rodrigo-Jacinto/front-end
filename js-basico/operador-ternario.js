//O Operador ternário subtitui o IF e else
//geralmente utilizando quando queremos passar o valor da condição para um variável

let resultado = 2 === 2 ? 'Os valores são igauis' : 'Os valores não são iguais';
console.log(resultado);
